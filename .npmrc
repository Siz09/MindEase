# Cypress configuration
# To skip Cypress binary installation in CI, set environment variable:
# CYPRESS_INSTALL_BINARY=0
# This prevents download failures when Cypress CDN is unavailable
cypress_skip_binary_install=false

# Retry configuration for network issues
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

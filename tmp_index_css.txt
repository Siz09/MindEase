@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  color-scheme: light;
}

/* Completely redesigned styling with modern light theme */
body {
  @apply bg-background text-text min-h-screen antialiased;
  font-feature-settings:
    'rlig' 1,
    'calt' 1;
}

html {
  scroll-behavior: smooth;
}

*:focus-visible {
  @apply outline-2 outline-offset-2 outline-accent;
}

a {
  @apply text-accent hover:text-accent-dark transition-colors;
}

:focus:not(:focus-visible) {
  @apply outline-none;
}

@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

h1,
h2,
h3,
h4,
h5,
h6 {
  @apply text-balance font-semibold;
}

p {
  @apply text-pretty leading-relaxed;
}

input[type='text'],
input[type='email'],
textarea {
  @apply transition-all duration-200;
}

input[type='text']:focus,
input[type='email']:focus,
textarea:focus {
  @apply border-accent ring-2 ring-accent/10;
}

button {
  @apply transition-all duration-200 cursor-pointer;
}

button:disabled {
  @apply cursor-not-allowed opacity-50;
}

.sr-only {
  @apply absolute w-1 h-1 p-0 -m-1 overflow-hidden clip-path-inset-50 border-0;
}

.sr-only-focusable:focus {
  @apply static w-auto h-auto p-0 m-0 overflow-visible clip-path-none;
}
